#!/bin/bash

# Init project
folders="apache56 apache70 apache71 nginx71 nginx70 nginx56"
cd magento/
for folder in $folders; do
    mkdir $folder
    cp .htaccess $folder
    cp install_magento $folder
    chmod +x $folder/install_magento
done
cd -
